{
  "name": "visualizations-monorepo",
  "workspaces": [
    "packages/*"
  ],
  "description": "Contiamo visualizations monorepo",
  "repository": "git@github.com:contiamo/operational-visualizations.git",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean --stream --parallel",
    "test": "lerna run test --stream --parallel",
    "start": "yarn workspace @operational/visualizations start"
  },
  "dependencies": {},
  "devDependencies": {
    "@operational/scripts": "^1.4.0",
    "@types/react": "^16.8.18",
    "@types/react-dom": "^16.8.4",
    "lerna": "^3.14.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "rimraf": "^2.6.3",
    "typescript": "^3.4.5"
  }
}
